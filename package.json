{
  "name": "atlas",
  "description": "Asana-class team project-management app foundation with Fastify, Next.js, Prisma, PostgreSQL, Redis, realtime updates, RBAC, and Docker local infrastructure.",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "cleanup:pending-uploads": "pnpm --filter @atlas/api cleanup:pending-uploads",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "migrate": "pnpm --filter @atlas/db migrate",
    "preflight": "node ./scripts/preflight.mjs",
    "preflight:local": "node ./scripts/preflight-local.mjs",
    "seed": "pnpm --filter @atlas/db seed",
    "smoke:demo:local": "node ./scripts/demo-smoke-local.mjs",
    "test": "turbo run test",
    "test:e2e": "pnpm --filter @atlas/api test:e2e",
    "test:integration": "pnpm --filter @atlas/api test:integration",
    "test:integration:local": "node ./scripts/test-integration-local.mjs",
    "test:unit": "turbo run test:unit",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@atlas/config": "workspace:*",
    "@eslint/js": "^9.39.4",
    "turbo": "^2.5.4",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.62.0"
  }
}
